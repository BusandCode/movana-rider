# Copy to .env and fill in real values.
# Exposed to app.config via app.json "extra" — wire these in app.config.ts if you switch from app.json.

API_BASE_URL=http://localhost:4000/api
SOCKET_URL=http://localhost:4000
GOOGLE_MAPS_API_KEY=
APP_ENV=development
